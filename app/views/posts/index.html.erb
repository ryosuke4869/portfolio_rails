<p>投稿用のページ</p>

<%= current_user.username %>


<%= link_to "投稿する", new_post_path %>
<%# <%= link_to "投稿したものを確認", post_path %>

<h2>投稿済み</h2>
  <div>
  <table>
    <thead>
      <tr>
        <th>id</th>
        <th>ルーム名</th>
      </tr>
    </thead>
    <tbody>
      <% @posts.each do |post| %>
        <tr>
    <td><%= post.id%></td>
    <td><%= post.desk_name%></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
